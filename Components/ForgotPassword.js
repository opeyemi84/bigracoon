import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
  TouchableOpacity,
} from "react-native";

const ForgotPassword = () => {
  return (
    <View style={StyleSheet.container}>
      <Text style={StyleSheet.title}>Password Reset</Text>
    </View>
  );
};
